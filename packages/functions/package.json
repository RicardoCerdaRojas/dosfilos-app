{
    "name": "@dosfilos/functions",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "main": "./src/index.ts",
    "scripts": {
        "build": "tsc",
        "serve": "npm run build && firebase emulators:start --only functions",
        "shell": "npm run build && firebase functions:shell",
        "deploy": "firebase deploy --only functions",
        "logs": "firebase functions:log"
    },
    "engines": {
        "node": "20"
    },
    "dependencies": {
        "@dosfilos/domain": "file:../domain",
        "@dosfilos/infrastructure": "file:../infrastructure",
        "@dosfilos/application": "file:../application",
        "firebase-admin": "^12.0.0",
        "firebase-functions": "^4.5.0"
    },
    "devDependencies": {
        "typescript": "^5.3.0"
    }
}